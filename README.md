# flask-web-app

flask app built by video guide

this repository will operate by opening the main.py file and running the debug program to show the running server through the terminal
server \* Running on http://127.0.0.1:5000

user will be able to login/logout through main features
as well as post, save and delete notes once logged in.
